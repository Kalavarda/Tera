<UserControl x:Class="Cards.UserControls.CardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderThickness="1" BorderBrush="#80808080" CornerRadius="1" Margin="2" Background="#01808080" MouseLeftButtonDown="OnMouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="_tbName"/>
            <TextBlock x:Name="_tbTarget" Grid.Row="1"/>
        </Grid>
    </Border>
</UserControl>
